namespace GuildGame.Data
{
    /// <summary>플레이어의 판정. 상시 노출된 두 버튼에 대응한다.</summary>
    public enum PlayerVerdict
    {
        ApproveComplete, // 의뢰 완료 승인
        RejectFail       // 의뢰 실패 판정
    }
}
