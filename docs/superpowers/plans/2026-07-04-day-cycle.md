# 하루 일과(Day Cycle) 시스템 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 일일 학생 검사 인원 제한(공통 기본값 + 일자별 SO 오버라이드)과 하루 전환(페이드아웃 → 날짜 표시 → 페이드인 → 일자별 시작 컷씬)을 기존 검사 루프에 추가한다.

**Architecture:** 데이터는 `DayConfigSO`(일자별) + `DayScheduleSO`(묶음, 딕셔너리 조회), 런타임은 `DayModel`(순수 C#), 플로우는 상태 머신에 `DayStartState`/`DayEndState` 추가, UI는 `GameContext` 이벤트를 구독하는 `DayTransitionView`. 스펙: `docs/superpowers/specs/2026-07-04-day-cycle-design.md`.

**Tech Stack:** Unity 6000.3.18f1, C#, DOTween, TextMeshPro, 커스텀 CSV 로컬라이제이션.

## Global Constraints

- 네임스페이스: 데이터 SO는 `GuildGame.Data`, 모델은 `GuildGame.Gameplay.Models`, 상태는 `GuildGame.Gameplay.Flow`, 뷰는 `GuildGame.UI`.
- MVC 경계: 뷰는 `GameContext` 이벤트 구독만, 모델 변경 금지. 상태는 뷰를 직접 호출하지 않고 `GameContext` 이벤트로 요청.
- 상태 전이는 반드시 `StateMachine.ChangeState(...)` / `GoNext()` 사용.
- 매직 넘버 금지: 연출 시간은 `UIAnimationSettingsSO`, 기획 수치는 SO에.
- 모든 표시 텍스트는 `Assets/Resources/Localization.csv`의 로컬라이제이션 키 사용.
- 클래스/필드 XML 주석은 기존 파일처럼 한국어로 작성.
- **테스트 인프라 없음**(asmdef 미사용, 전부 Assembly-CSharp): 각 태스크의 검증은 Unity 에디터 컴파일 확인(콘솔 에러 0건), 최종 태스크에서 플레이 모드 수동 검증. Unity MCP 도구(`mcp__unity-mcp__Unity_GetConsoleLogs`)로 콘솔을 확인한다 (사용 전 ToolSearch로 스키마 로드 필요).
- 커밋 메시지 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 추가.

---

### Task 1: 데이터 계층 — DayConfigSO + DayScheduleSO

**Files:**
- Create: `Assets/Scripts/Data/DayConfigSO.cs`
- Create: `Assets/Scripts/Data/DayScheduleSO.cs`

**Interfaces:**
- Consumes: 기존 `GuildGame.Data.CutsceneSO`.
- Produces: `DayConfigSO { int day; bool overrideStudentLimit; int studentLimit; CutsceneSO dayStartCutscene; }`, `DayScheduleSO { int defaultStudentLimit; DayConfigSO GetConfig(int day); int GetStudentLimit(int day); }` — Task 2의 `DayModel`이 사용.

- [ ] **Step 1: DayConfigSO 작성**

`Assets/Scripts/Data/DayConfigSO.cs`:

```csharp
using UnityEngine;

namespace GuildGame.Data
{
    /// <summary>
    /// 하루치 기획 데이터. 인원 제한 오버라이드와 하루 시작 이벤트(컷씬)를 정의한다.
    /// 이 SO가 없는 날짜는 DayScheduleSO의 공통 기본값으로 진행된다.
    /// </summary>
    [CreateAssetMenu(fileName = "DayConfig", menuName = "GuildGame/Day Config", order = 40)]
    public class DayConfigSO : ScriptableObject
    {
        [Min(1)]
        [Tooltip("몇 일차 데이터인지 (1일차 = 1)")]
        public int day = 1;

        [Header("인원 제한")]
        [Tooltip("체크하면 이 날은 공통 기본값 대신 아래 studentLimit을 사용")]
        public bool overrideStudentLimit;

        [Min(1)]
        [Tooltip("이 날의 검사 인원 제한 (overrideStudentLimit이 true일 때만 적용)")]
        public int studentLimit = 4;

        [Header("하루 시작 이벤트")]
        [Tooltip("하루 시작(페이드인 완료) 후 재생할 컷씬. 없으면 바로 학생 입장")]
        public CutsceneSO dayStartCutscene;
    }
}
```

- [ ] **Step 2: DayScheduleSO 작성**

`Assets/Scripts/Data/DayScheduleSO.cs`:

```csharp
using System.Collections.Generic;
using UnityEngine;

namespace GuildGame.Data
{
    /// <summary>
    /// 전체 일자별 데이터 묶음. 공통 인원 제한 기본값과 일자별 DayConfigSO 목록을 갖고,
    /// day를 key로 하는 딕셔너리로 조회한다. 등록되지 않은 날짜는 공통 기본값으로 진행된다.
    /// </summary>
    [CreateAssetMenu(fileName = "DaySchedule", menuName = "GuildGame/Day Schedule", order = 41)]
    public class DayScheduleSO : ScriptableObject
    {
        [Min(1)]
        [Tooltip("모든 날짜 공통 검사 인원 제한 (일자별 오버라이드가 없을 때 사용)")]
        public int defaultStudentLimit = 8;

        [Tooltip("일자별 데이터. day 중복 시 먼저 등록된 항목이 우선")]
        [SerializeField] private List<DayConfigSO> _days = new();

        private Dictionary<int, DayConfigSO> _byDay;

        /// <summary>해당 일자의 DayConfigSO를 반환한다. 등록되지 않은 날짜면 null.</summary>
        public DayConfigSO GetConfig(int day)
        {
            EnsureIndex();
            return _byDay.TryGetValue(day, out DayConfigSO config) ? config : null;
        }

        /// <summary>해당 일자의 검사 인원 제한. 오버라이드가 없으면 공통 기본값.</summary>
        public int GetStudentLimit(int day)
        {
            DayConfigSO config = GetConfig(day);
            if (config == null || !config.overrideStudentLimit)
                return defaultStudentLimit;

            return Mathf.Max(1, config.studentLimit);
        }

        private void EnsureIndex()
        {
            if (_byDay != null)
                return;

            _byDay = new Dictionary<int, DayConfigSO>();
            foreach (DayConfigSO config in _days)
            {
                if (config == null)
                {
                    Debug.LogWarning($"[DaySchedule] {name}: null 항목이 있어 무시함.");
                    continue;
                }

                if (_byDay.ContainsKey(config.day))
                {
                    Debug.LogWarning($"[DaySchedule] {name}: day {config.day} 중복. '{config.name}' 무시함.");
                    continue;
                }

                if (config.overrideStudentLimit && config.studentLimit <= 0)
                    Debug.LogWarning($"[DaySchedule] {name}: '{config.name}' studentLimit {config.studentLimit} <= 0, 1로 클램프됨.");

                _byDay.Add(config.day, config);
            }
        }

        private void OnValidate()
        {
            // 에디터에서 목록 수정 시 다음 조회 때 딕셔너리 재빌드
            _byDay = null;
        }
    }
}
```

- [ ] **Step 3: 컴파일 확인**

Unity 에디터가 리컴파일하도록 포커스(또는 `mcp__unity-mcp__Unity_RunCommand`로 `AssetDatabase.Refresh`) 후 `mcp__unity-mcp__Unity_GetConsoleLogs`로 확인.
Expected: 컴파일 에러 0건.

- [ ] **Step 4: 커밋**

```bash
git add Assets/Scripts/Data/DayConfigSO.cs Assets/Scripts/Data/DayScheduleSO.cs Assets/Scripts/Data/*.meta
git commit -m "feat(data): add DayConfigSO and DayScheduleSO for day cycle"
```

---

### Task 2: DayModel + GameContext 확장 + DeskController 주입

**Files:**
- Create: `Assets/Scripts/Gameplay/Models/DayModel.cs`
- Modify: `Assets/Scripts/Gameplay/Flow/GameContext.cs` (의존성 1개, 이벤트 3개 추가)
- Modify: `Assets/Scripts/Gameplay/Flow/DeskController.cs` (필드 추가 + 모델 생성/주입)

**Interfaces:**
- Consumes: Task 1의 `DayScheduleSO.GetConfig(int)` / `GetStudentLimit(int)`, 기존 `ObservableProperty<T>`.
- Produces:
  - `DayModel { ObservableProperty<int> CurrentDay; ObservableProperty<int> ProcessedToday; int TodayLimit; bool IsQuotaReached; DayConfigSO TodayConfig; void CountProcessed(); void AdvanceDay(); }`
  - `GameContext.Day` (DayModel), `event Action<int> DayStarted`, `event Action<int> DayEnded`, `event Action<CutsceneSO> CutscenePlayRequested`와 각각의 `RaiseDayStarted(int)` / `RaiseDayEnded(int)` / `RequestCutscene(CutsceneSO)` — Task 3(뷰), Task 4(상태), Task 5(배선)가 사용.

- [ ] **Step 1: DayModel 작성**

`Assets/Scripts/Gameplay/Models/DayModel.cs`:

```csharp
using GuildGame.Core;
using GuildGame.Data;

namespace GuildGame.Gameplay.Models
{
    /// <summary>
    /// 날짜 진행 스탯. 현재 일차와 오늘 처리한 학생 수를 반응형으로 노출하고,
    /// 일자별 인원 제한은 DayScheduleSO에서 조회한다. 변경은 CountProcessed/AdvanceDay로만 한다.
    /// </summary>
    public class DayModel
    {
        private readonly DayScheduleSO _schedule;

        public ObservableProperty<int> CurrentDay { get; }
        public ObservableProperty<int> ProcessedToday { get; }

        /// <summary>오늘의 검사 인원 제한.</summary>
        public int TodayLimit => _schedule.GetStudentLimit(CurrentDay.Value);

        /// <summary>오늘 제한 인원을 모두 처리했는지.</summary>
        public bool IsQuotaReached => ProcessedToday.Value >= TodayLimit;

        /// <summary>오늘의 일자별 데이터. 등록되지 않은 날짜면 null.</summary>
        public DayConfigSO TodayConfig => _schedule.GetConfig(CurrentDay.Value);

        public DayModel(DayScheduleSO schedule, int startDay = 1)
        {
            _schedule = schedule;
            CurrentDay = new ObservableProperty<int>(startDay);
            ProcessedToday = new ObservableProperty<int>(0);
        }

        /// <summary>학생 1명 처리 완료를 기록한다(Resolution에서 호출).</summary>
        public void CountProcessed()
        {
            ProcessedToday.Value += 1;
        }

        /// <summary>다음 날로 넘어간다. 처리 인원은 리셋된다.</summary>
        public void AdvanceDay()
        {
            CurrentDay.Value += 1;
            ProcessedToday.Value = 0;
        }
    }
}
```

- [ ] **Step 2: GameContext에 Day 의존성 + 이벤트 추가**

`Assets/Scripts/Gameplay/Flow/GameContext.cs` 수정. 프로퍼티 블록에 추가:

```csharp
public DayModel Day { get; }
```

생성자 시그니처를 다음으로 변경 (`reputation` 뒤에 `day` 추가):

```csharp
public GameContext(
    ILocalizationProvider localization,
    IStudentCaseGenerator generator,
    IJudgementService judgement,
    GameBalanceSO balance,
    ReputationModel reputation,
    DayModel day,
    UIAnimationSettingsSO uiAnimationSettings,
    bool loopAfterResolution = true,
    bool isTutorial = false)
```

생성자 본문에 `Reputation = reputation;` 다음 줄로 `Day = day;` 추가.

출력 이벤트 블록(`StudentExitRequested` 근처)에 추가:

```csharp
public event Action<int> DayStarted;
public event Action<int> DayEnded;
public event Action<CutsceneSO> CutscenePlayRequested;

public void RaiseDayStarted(int day) => DayStarted?.Invoke(day);
public void RaiseDayEnded(int day) => DayEnded?.Invoke(day);
public void RequestCutscene(CutsceneSO cutscene) => CutscenePlayRequested?.Invoke(cutscene);
```

- [ ] **Step 3: DeskController에 DayScheduleSO 필드 + DayModel 생성/주입**

`Assets/Scripts/Gameplay/Flow/DeskController.cs` 수정.

필드 추가 (`_uiAnimationSettings` 아래):

```csharp
[SerializeField] private DayScheduleSO _daySchedule;
```

`Start()`의 null 가드를 다음으로 확장:

```csharp
if (_balance == null || _studentDatabase == null || _daySchedule == null)
{
    Debug.LogError("[GuildDesk] GameBalanceSO, StudentDatabaseSO or DayScheduleSO is missing.");
    enabled = false;
    return;
}
```

`var reputation = ...` 다음 줄에 모델 생성 추가, `GameContext` 생성 호출에 `day` 전달:

```csharp
var reputation = new ReputationModel(_balance.startingReputation);
var day = new DayModel(_daySchedule);

_context = new GameContext(
    localization,
    generator,
    judgement,
    _balance,
    reputation,
    day,
    _uiAnimationSettings,
    loopAfterResolution: !_runTutorialOnStart,
    isTutorial: _runTutorialOnStart);
```

- [ ] **Step 4: 컴파일 확인**

`mcp__unity-mcp__Unity_GetConsoleLogs`로 확인.
Expected: 컴파일 에러 0건.

- [ ] **Step 5: 커밋**

```bash
git add Assets/Scripts/Gameplay/Models/DayModel.cs Assets/Scripts/Gameplay/Models/*.meta Assets/Scripts/Gameplay/Flow/GameContext.cs Assets/Scripts/Gameplay/Flow/DeskController.cs
git commit -m "feat(gameplay): add DayModel and day events to GameContext"
```

---

### Task 3: DayTransitionView + 연출 설정 + 로컬라이제이션 키

**Files:**
- Modify: `Assets/Scripts/Data/UIAnimationSettingsSO.cs` (Day Transition 항목 3개 추가)
- Modify: `Assets/Resources/Localization.csv` (`day_label` 키 추가)
- Create: `Assets/Scripts/UI/DayTransitionView.cs`

**Interfaces:**
- Consumes: Task 2의 `GameContext.DayStarted` / `DayEnded` 이벤트, `ILocalizationProvider.GetFormatted(key, args)`.
- Produces: `UIAnimationSettingsSO.dayFadeOutDuration / dayLabelHoldDuration / dayFadeInDuration` (float) — Task 4의 상태들이 사용. 씬 오브젝트는 Task 6에서 배치.

- [ ] **Step 1: UIAnimationSettingsSO에 Day Transition 항목 추가**

`Assets/Scripts/Data/UIAnimationSettingsSO.cs`의 `studentExit` 헤더 앞에 추가:

```csharp
[Header("Day Transition")]
[Tooltip("하루 종료 시 화면이 어두워지는 페이드아웃 시간(초)")]
[Min(0f)]
public float dayFadeOutDuration = 0.8f;

[Tooltip("검은 화면에서 날짜(Day N)를 표시하고 유지하는 시간(초)")]
[Min(0f)]
public float dayLabelHoldDuration = 1.5f;

[Tooltip("다음 날 화면이 밝아지는 페이드인 시간(초)")]
[Min(0f)]
public float dayFadeInDuration = 0.8f;
```

- [ ] **Step 2: Localization.csv에 day_label 키 추가**

`Assets/Resources/Localization.csv`의 `ui_verdict_fail` 줄 아래에 추가:

```csv
day_label,{0}일차,Day {0}
```

- [ ] **Step 3: DayTransitionView 작성**

`Assets/Scripts/UI/DayTransitionView.cs`:

```csharp
using DG.Tweening;
using GuildGame.Data;
using TMPro;
using UnityEngine;

namespace GuildGame.UI
{
    /// <summary>
    /// 하루 전환 오버레이. DayEnded에 페이드아웃, DayStarted에 날짜 표시 후 페이드인한다.
    /// 전환 중에는 입력을 차단한다. 뷰는 이벤트 구독만 하고 모델을 변경하지 않는다.
    /// </summary>
    public class DayTransitionView : UIViewBase
    {
        [SerializeField] private CanvasGroup _overlay;
        [SerializeField] private TMP_Text _dayLabel;

        private Tween _tween;

        protected override void OnBind()
        {
            Context.DayEnded += OnDayEnded;
            Context.DayStarted += OnDayStarted;

            SetOverlay(0f, false);
            SetLabelVisible(false);
        }

        private void OnDayEnded(int day)
        {
            if (_overlay == null)
                return;

            _tween?.Kill();
            SetLabelVisible(false);
            _overlay.blocksRaycasts = true;
            _tween = _overlay.DOFade(1f, GetFadeOutDuration());
        }

        private void OnDayStarted(int day)
        {
            if (_overlay == null)
                return;

            _tween?.Kill();
            SetOverlay(1f, true);

            if (_dayLabel != null)
                _dayLabel.text = Context.Localization.GetFormatted("day_label", day);
            SetLabelVisible(true);

            _tween = DOTween.Sequence()
                .AppendInterval(GetHoldDuration())
                .AppendCallback(() => SetLabelVisible(false))
                .Append(_overlay.DOFade(0f, GetFadeInDuration()))
                .OnComplete(() => _overlay.blocksRaycasts = false);
        }

        private void SetOverlay(float alpha, bool blocksRaycasts)
        {
            if (_overlay == null)
                return;

            _overlay.alpha = alpha;
            _overlay.blocksRaycasts = blocksRaycasts;
        }

        private void SetLabelVisible(bool visible)
        {
            if (_dayLabel != null)
                _dayLabel.gameObject.SetActive(visible);
        }

        private float GetFadeOutDuration()
        {
            UIAnimationSettingsSO settings = Context.UIAnimationSettings;
            return settings != null ? settings.dayFadeOutDuration : 0.8f;
        }

        private float GetHoldDuration()
        {
            UIAnimationSettingsSO settings = Context.UIAnimationSettings;
            return settings != null ? settings.dayLabelHoldDuration : 1.5f;
        }

        private float GetFadeInDuration()
        {
            UIAnimationSettingsSO settings = Context.UIAnimationSettings;
            return settings != null ? settings.dayFadeInDuration : 0.8f;
        }

        private void OnDestroy()
        {
            _tween?.Kill();
            if (Context != null)
            {
                Context.DayEnded -= OnDayEnded;
                Context.DayStarted -= OnDayStarted;
            }
        }
    }
}
```

설계 노트: 오버레이는 투명(alpha 0)으로 시작한다. 일반 모드는 첫 프레임에 `DayStarted`가 발행되어 즉시 검은 화면 + 날짜 표시로 시작하고, 튜토리얼 모드는 `DayStarted`가 발행되지 않으므로 오버레이가 계속 투명하게 유지된다.

- [ ] **Step 4: 컴파일 확인**

`mcp__unity-mcp__Unity_GetConsoleLogs`로 확인.
Expected: 컴파일 에러 0건.

- [ ] **Step 5: 커밋**

```bash
git add Assets/Scripts/Data/UIAnimationSettingsSO.cs Assets/Scripts/UI/DayTransitionView.cs Assets/Scripts/UI/*.meta Assets/Resources/Localization.csv
git commit -m "feat(ui): add day transition overlay view and timing settings"
```

---

### Task 4: DayStartState + DayEndState + CutsceneDialogueRunner 연동

**Files:**
- Create: `Assets/Scripts/Gameplay/Flow/DayEndState.cs`
- Create: `Assets/Scripts/Gameplay/Flow/DayStartState.cs`
- Modify: `Assets/Scripts/UI/CutsceneDialogueRunner.cs` (`CutscenePlayRequested` 구독 + `Stop()` 수정)

**Interfaces:**
- Consumes: Task 2의 `Context.Day`(`CurrentDay`, `AdvanceDay()`, `TodayConfig`), `RaiseDayStarted/RaiseDayEnded/RequestCutscene`, Task 3의 `UIAnimationSettingsSO` day 항목들, 기존 `Context.CutsceneEnded` 이벤트.
- Produces: `DayEndState`, `DayStartState` (둘 다 `GameStateBase` 상속, `Next` 프로퍼티로 배선) — Task 5가 배선.

- [ ] **Step 1: CutsceneDialogueRunner의 Stop()이 재생 중일 때만 CutsceneEnded를 발행하도록 수정**

현재 `Stop()`은 재생 중이 아니어도 `CutsceneEnded`를 발행한다. `Play()`가 내부에서 `Stop()`을 먼저 호출하므로, `CutsceneEnded`를 기다리는 `DayStartState`가 컷씬 시작 시점에 잘못 깨어날 수 있다. `Assets/Scripts/UI/CutsceneDialogueRunner.cs`의 `Stop()`을 다음으로 교체:

```csharp
public void Stop()
{
    if (_playRoutine == null)
        return;

    StopCoroutine(_playRoutine);
    _playRoutine = null;
    Context?.RaiseCutsceneEnded();
}
```

- [ ] **Step 2: Stop()의 기존 호출자가 이 변경에 의존하지 않는지 확인**

Run: `grep -rn "\.Stop()" Assets/Scripts --include=*.cs`
Expected: `CutsceneDialogueRunner.Play()` 내부 호출과 `DeskController`의 `_tutorialHighlight?.Stop()`(별개 클래스)만 검색됨. 그 외 호출자가 있으면 "재생 중이 아닐 때도 CutsceneEnded가 필요한지" 확인 후 진행.

- [ ] **Step 3: CutsceneDialogueRunner가 CutscenePlayRequested를 구독**

같은 파일의 `OnBind()`를 다음으로 교체:

```csharp
protected override void OnBind()
{
    Context.CutscenePlayRequested += Play;

    if (_autoPlayOnBind && _playOnBind != null)
        Play(_playOnBind);
}
```

`OnDestroy()`를 다음으로 교체:

```csharp
private void OnDestroy()
{
    if (Context != null)
        Context.CutscenePlayRequested -= Play;

    if (_playRoutine != null)
        StopCoroutine(_playRoutine);

    _playRoutine = null;
}
```

- [ ] **Step 4: DayEndState 작성**

`Assets/Scripts/Gameplay/Flow/DayEndState.cs`:

```csharp
using DG.Tweening;
using GuildGame.Core;
using GuildGame.Data;

namespace GuildGame.Gameplay.Flow
{
    /// <summary>
    /// 하루 종료 상태. 페이드아웃 이벤트를 발행하고, 페이드가 끝나면 날짜를 넘긴 뒤
    /// 다음 날 시작 상태로 전이한다.
    /// </summary>
    public class DayEndState : GameStateBase
    {
        private Tween _flowTween;

        public DayEndState(GameContext context, StateMachine machine) : base(context, machine) { }

        public override void Enter()
        {
            Context.RaiseDayEnded(Context.Day.CurrentDay.Value);

            UIAnimationSettingsSO settings = Context.UIAnimationSettings;
            float fadeOutDuration = settings != null ? settings.dayFadeOutDuration : 0.8f;

            _flowTween?.Kill();
            _flowTween = DOTween.Sequence()
                .AppendInterval(fadeOutDuration)
                .AppendCallback(() =>
                {
                    Context.Day.AdvanceDay();
                    GoNext();
                });
        }

        public override void Exit()
        {
            _flowTween?.Kill();
            _flowTween = null;
        }
    }
}
```

- [ ] **Step 5: DayStartState 작성**

`Assets/Scripts/Gameplay/Flow/DayStartState.cs`:

```csharp
using DG.Tweening;
using GuildGame.Core;
using GuildGame.Data;

namespace GuildGame.Gameplay.Flow
{
    /// <summary>
    /// 하루 시작 상태. 날짜 표시/페이드인 이벤트를 발행하고, 연출이 끝나면
    /// 당일 시작 컷씬이 있을 경우 재생 완료를 기다린 뒤 학생 입장 상태로 전이한다.
    /// </summary>
    public class DayStartState : GameStateBase
    {
        private Tween _flowTween;
        private bool _waitingCutscene;

        public DayStartState(GameContext context, StateMachine machine) : base(context, machine) { }

        public override void Enter()
        {
            Context.RaiseDayStarted(Context.Day.CurrentDay.Value);

            UIAnimationSettingsSO settings = Context.UIAnimationSettings;
            float holdDuration = settings != null ? settings.dayLabelHoldDuration : 1.5f;
            float fadeInDuration = settings != null ? settings.dayFadeInDuration : 0.8f;

            _flowTween?.Kill();
            _flowTween = DOTween.Sequence()
                .AppendInterval(holdDuration + fadeInDuration)
                .AppendCallback(OnFadeInComplete);
        }

        public override void Exit()
        {
            _flowTween?.Kill();
            _flowTween = null;

            if (_waitingCutscene)
            {
                Context.CutsceneEnded -= OnCutsceneEnded;
                _waitingCutscene = false;
            }
        }

        private void OnFadeInComplete()
        {
            DayConfigSO config = Context.Day.TodayConfig;
            CutsceneSO cutscene = config != null ? config.dayStartCutscene : null;

            if (cutscene == null)
            {
                GoNext();
                return;
            }

            _waitingCutscene = true;
            Context.CutsceneEnded += OnCutsceneEnded;
            Context.RequestCutscene(cutscene);
        }

        private void OnCutsceneEnded()
        {
            _waitingCutscene = false;
            Context.CutsceneEnded -= OnCutsceneEnded;
            GoNext();
        }
    }
}
```

- [ ] **Step 6: 컴파일 확인**

`mcp__unity-mcp__Unity_GetConsoleLogs`로 확인.
Expected: 컴파일 에러 0건.

- [ ] **Step 7: 커밋**

```bash
git add Assets/Scripts/Gameplay/Flow/DayEndState.cs Assets/Scripts/Gameplay/Flow/DayStartState.cs Assets/Scripts/Gameplay/Flow/*.meta Assets/Scripts/UI/CutsceneDialogueRunner.cs
git commit -m "feat(flow): add day start/end states and cutscene play request handling"
```

---

### Task 5: ResolutionState 분기 + DeskController 상태 배선

**Files:**
- Modify: `Assets/Scripts/Gameplay/Flow/ResolutionState.cs`
- Modify: `Assets/Scripts/Gameplay/Flow/DeskController.cs` (`Start()`의 상태 생성/배선 부분)

**Interfaces:**
- Consumes: Task 2의 `Context.Day.CountProcessed()` / `IsQuotaReached`, Task 4의 `DayEndState` / `DayStartState`.
- Produces: `ResolutionState.DayEnd` (`IState`, 컨트롤러가 배선). 일반 모드 첫 상태 = `DayStartState`.

- [ ] **Step 1: ResolutionState에 처리 카운트 + 분기 추가**

`Assets/Scripts/Gameplay/Flow/ResolutionState.cs` 수정.

`DayEnd` 프로퍼티 추가 (클래스 상단, `_flowTween` 필드 아래):

```csharp
/// <summary>오늘 제한 인원을 모두 처리했을 때 전이할 상태(컨트롤러가 배선).</summary>
public IState DayEnd { get; set; }
```

`Enter()`에서 평판 적용 직후 처리 카운트 추가:

```csharp
GameBalanceSO.OutcomeInfo info = Context.Balance.GetInfo(outcome);
Context.Reputation.Apply(info.reputationDelta);
Context.Day.CountProcessed();
```

시퀀스 마지막 분기를 다음으로 교체 (기존 `if (Context.LoopAfterResolution) sequence.AppendCallback(GoNext);` 부분):

```csharp
if (Context.LoopAfterResolution)
    sequence.AppendCallback(() =>
    {
        if (Context.Day.IsQuotaReached && DayEnd != null)
            Machine.ChangeState(DayEnd);
        else
            GoNext();
    });
```

파일 상단 using에 `GuildGame.Core`가 이미 있으므로 추가 using 불필요 (`IState`는 `GuildGame.Core`).

- [ ] **Step 2: DeskController 상태 배선 변경**

`Assets/Scripts/Gameplay/Flow/DeskController.cs`의 `Start()`에서 상태 생성/배선 부분을 다음으로 교체:

```csharp
_machine = new StateMachine();
var dayStart = new DayStartState(_context, _machine);
var enter = new StudentEnterState(_context, _machine);
var inspection = new InspectionState(_context, _machine);
var resolution = new ResolutionState(_context, _machine);
var dayEnd = new DayEndState(_context, _machine);

dayStart.Next = enter;
enter.Next = inspection;
inspection.Next = resolution;
resolution.Next = enter;
resolution.DayEnd = dayEnd;
dayEnd.Next = dayStart;

BindViews();
_machine.ChangeState(_runTutorialOnStart ? (IState)enter : dayStart);
```

파일 상단 using에 `GuildGame.Core`가 이미 있는지 확인하고 없으면 추가 (`IState` 캐스트에 필요).

- [ ] **Step 3: 컴파일 확인**

`mcp__unity-mcp__Unity_GetConsoleLogs`로 확인.
Expected: 컴파일 에러 0건.

- [ ] **Step 4: 커밋**

```bash
git add Assets/Scripts/Gameplay/Flow/ResolutionState.cs Assets/Scripts/Gameplay/Flow/DeskController.cs
git commit -m "feat(flow): route resolution to day end when daily quota is reached"
```

---

### Task 6: 에셋/씬 셋업 + 플레이 모드 수동 검증

Unity 에디터가 열려 있어야 한다. 에셋 생성과 씬 편집은 `mcp__unity-mcp__Unity_RunCommand`로 수행하거나, 불가하면 아래 내용을 사용자에게 수동 작업으로 안내한다.

**Files:**
- Create: `Assets/Data/Days/DaySchedule.asset`
- Create: `Assets/Data/Days/Day01.asset`
- Modify: 메인 게임 씬 (DeskController가 있는 씬) — `DayTransitionView` 오브젝트 추가, `DeskController._daySchedule` 할당

**Interfaces:**
- Consumes: Task 1~5의 전부.
- Produces: 동작하는 하루 전환 루프.

- [ ] **Step 1: 에셋 생성**

1. `Assets/Data/Days/` 폴더 생성.
2. `DaySchedule.asset` 생성 (메뉴 `GuildGame/Day Schedule`): `defaultStudentLimit = 8`.
3. `Day01.asset` 생성 (메뉴 `GuildGame/Day Config`): `day = 1`, `overrideStudentLimit = true`, `studentLimit = 2` (검증용 소인원. 검증 후 기획값으로 조정), `dayStartCutscene`은 기존 컷씬 SO가 있으면 하나 할당, 없으면 비움.
4. `DaySchedule.asset`의 `_days`에 `Day01.asset` 등록.

- [ ] **Step 2: 씬 배선**

1. 메인 씬의 UI Canvas 아래 최상단(가장 마지막 sibling, 모든 UI 위에 그려지도록)에 `DayTransitionOverlay` GameObject 생성:
   - `RectTransform` 앵커 stretch-stretch, offset 0 (전체 화면).
   - 검은색 `Image` 컴포넌트 (color: `#000000`, alpha 255).
   - `CanvasGroup` 컴포넌트 (alpha는 뷰가 제어).
   - `DayTransitionView` 컴포넌트: `_overlay` = 자신의 CanvasGroup, `_dayLabel` = 자식으로 만든 TMP 텍스트 (중앙 정렬, 폰트는 씬의 기존 TMP 텍스트와 동일한 것 사용).
2. `DeskController`의 `_daySchedule`에 `DaySchedule.asset` 할당.
3. 씬 저장.

- [ ] **Step 3: 플레이 모드 검증 — 기본 하루 전환**

플레이 모드 진입 (튜토리얼 모드 off) 후 확인:

1. 시작 시 검은 화면에 "1일차"(en이면 "Day 1") 표시 → 페이드인 → (컷씬 할당 시 컷씬 대사 진행 →) 학생 입장.
2. 학생 2명 판정 완료(Day01 제한 = 2) → 학생 퇴장 후 페이드아웃 → "2일차" 표시 → 페이드인 → 학생 입장.
3. 2일차는 오버라이드가 없으므로 8명째 판정까지 하루가 유지되는지 확인 (시간이 길면 `defaultStudentLimit`을 임시로 2로 낮춰 확인 후 복원).
4. 전환 중(검은 화면) 버튼 클릭이 차단되는지 확인.
5. 콘솔에 에러/경고 없는지 `mcp__unity-mcp__Unity_GetConsoleLogs`로 확인.

- [ ] **Step 4: 플레이 모드 검증 — 튜토리얼 모드 회귀**

`DeskController`의 `_runTutorialOnStart`를 켜고 플레이:

1. 검은 오버레이/날짜 표시가 나타나지 않고 기존 튜토리얼 흐름이 그대로 동작하는지 확인.
2. 확인 후 `_runTutorialOnStart` 원복.

- [ ] **Step 5: 검증용 수치 원복 및 커밋**

`Day01.asset`의 `studentLimit` 등 검증용 임시 수치를 기획 의도값으로 되돌린 뒤:

```bash
git add Assets/Data/Days Assets/Data/Days.meta
git add -A Assets/Scenes
git commit -m "feat(assets): add day schedule assets and wire day transition overlay"
```

---

## 검증 요약

| 스펙 요구사항 | 커버 태스크 |
| --- | --- |
| 공통 기본값 + 일자별 오버라이드 딕셔너리 조회 | Task 1 |
| 인원 제한 도달 시 페이드아웃 → 다음 날 | Task 4, 5, 6 |
| 다음 날 날짜 표시 + 페이드인 | Task 3, 4, 6 |
| 일자별 시작 이벤트(컷씬) 실행 | Task 1, 4, 6 |
| 튜토리얼 모드 무영향 | Task 3(투명 시작), 5(첫 상태 분기), 6(회귀 검증) |
| 에러 처리(중복 키, null, limit<=0, SO 미할당) | Task 1, 2 |
